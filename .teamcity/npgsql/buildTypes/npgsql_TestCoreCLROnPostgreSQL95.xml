<?xml version="1.0" encoding="UTF-8"?>
<build-type xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" uuid="f1850150-0e2c-4200-9564-3cacec070421" xsi:noNamespaceSchemaLocation="http://www.jetbrains.com/teamcity/schemas/9.0/project-config.xsd">
  <name>Test CoreCLR on PostgreSQL 9.5</name>
  <description />
  <settings>
    <options>
      <option name="checkoutMode" value="ON_AGENT" />
    </options>
    <build-runners>
<!-- This is a total hack to allow running CoreCLR tests in 3.1 while allowing 3.0 (which doesn't support CoreCLR) to build without them.
     TeamCity doesn't currently support different build chain dependency graphs - it always picks up the dependency graph from the
     default branch (see https://youtrack.jetbrains.com/issue/TW-42533).
     What we can do is define the "Run CoreCLR Tests" config in a different way across branches - so the 3.0 "version" is a noop.
-->
    </build-runners>
    <vcs-settings>
      <vcs-entry-ref root-id="npgsql_github" />
    </vcs-settings>
    <requirements />
    <build-triggers />
    <cleanup />
  </settings>
</build-type>

