machine:
  services:
    - docker

dependencies:
  override:
     - docker build -f test/Dockerfile.test -t local/tests .

test:
  override:
    - docker run local/tests dnx -p test/EntityFramework7.Npgsql.Tests/project.json test
